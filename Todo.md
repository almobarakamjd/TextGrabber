# Todo — TextGrabber

نقاط مؤجلة للنقاش أو التنفيذ لاحقا (لم يُطلب تنفيذها بعد):

- [ ] استبدال أيقونة التطبيق (Vector Drawable البسيطة الحالية) بأيقونة تصميم نهائية.
- [ ] إضافة زر "مسح القائمة" لمسح آخر النصوص المنسوخة من الواجهة (الدالة `CopiedTextStore.clear()` جاهزة بالفعل ولم تُربط بزر بعد).
- [ ] النظر في الانتقال من `SnapshotStateList` في الذاكرة إلى قاعدة بيانات Room إن أراد المستخدم حفظ السجل بعد إغلاق التطبيق.
- [ ] توليد سكربتات `gradlew` / `gradlew.bat` القياسية (لدينا `gradle-wrapper.jar` فقط حاليا؛ البناء تم عبر تشغيل Gradle 8.14.5 المخزن محليا في `~/.gradle/wrapper/dists` مباشرة). ستُنشأ سكربتات gradlew تلقائيا عند فتح المشروع في Android Studio.
- [x] بناء APK (release) ورفعه: تم فعليا بأمر صريح من أبو فيصل بتاريخ 2026-09-04. الأمر المستخدم كان تشغيل Gradle مباشرة (`gradle assembleRelease`) وليس `./gradlew` لعدم وجود سكربتات wrapper بعد.
- [x] رفع المشروع إلى GitHub وإنشاء Release مع إرفاق APK: تم بتاريخ 2026-09-04. الرابط: https://github.com/almobarakamjd/TextGrabber/releases/tag/v1.0.0
- [ ] استبدال توقيع الإصدار (حاليا يستخدم مفتاح Debug القياسي) بمفتاح توقيع رسمي (Keystore) عند الرغبة بالنشر على متجر Google Play مستقبلا.
- [ ] تأكيد ميداني لإصلاح v1.3.1: بعد تثبيت الإصدار الجديد، مراقبة سجلّ الخدمة عند تكرار العطل للتأكد من السبب المرجّح:
      `adb logcat -s TextGrabberService:*` — أي سطر "فشل إضافة الزر العائم" أو "مرجع زر عائم قديم غير مرتبط بنافذة" يؤكد التشخيص.
- [ ] احتمال ثانوي لم يُستبعد بعد: بعض واجهات الشركات (سامسونج/شاومي) قد تُسقط إذن خدمة إمكانية الوصول أو تقيّدها بعد تحديث يغيّر قدراتها (إضافة `canTakeScreenshot`). إن تكرر العطل رغم إصلاحات v1.3.1، يُفحص `adb shell settings get secure enabled_accessibility_services` قبل العطل وبعده.
- [ ] تقليل استهلاك الذاكرة في مسار "صورة": حاليا تُنسخ لقطة الشاشة كاملة إلى `Bitmap` بصيغة ARGB_8888 قبل القصّ، وهو تخصيص كبير داخل عملية الخدمة يرفع احتمال قتلها تحت ضغط الذاكرة.
- [ ] **اختبار ميداني للالتقاط الممتد (v1.4.0)** — المنطق مبني بلا اختبار على جهاز حقيقي. النقاط المرشّحة للمشكلات: التطبيقات التي ترفض `dispatchGesture`، والصفحات ذات الرأس الثابت (قد يتكرر الرأس في الصورة المدموجة)، والتمرير بقصور ذاتي (fling) الذي قد يتجاوز مسافة الإيماءة فيضيع محتوى بين صفحتين.
- [ ] ضبط دقة OCR العربي بعد التجربة: إن كانت النتيجة ضعيفة، تُجرَّب `tessdata_best` للعربية (نحو 15 ميجابايت إضافية) أو تُضاف عتبة ثنائية (binarization) قبل التمرير على المحرّك.
- [ ] مراجعة عتبة `MIN_BAND_HEIGHT_PX = 48` في `SelectionAnalyzer`: إن ظهر OCR يعمل على فراغات بين الفقرات بلا داعٍ فتُرفع، وإن فاتته صور صغيرة فتُخفض.

